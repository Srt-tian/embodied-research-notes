# GitHub Pages export

This repository keeps the source private while producing a portable static export for publication under:

`https://srt-tian.github.io/embodied-research-notes/`

The existing Sites build remains unchanged. The static export is enabled only when `GITHUB_PAGES=true`.
